<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.goodee.JoinTree.mapper.MeetRoomMapper">
	<!-- 경영지원 회의실 목록 조회 -->
	<!-- db createdate 오타 수정하기 -->
	<select id="selectMeetRoomAll" parameterType="java.util.Map" resultType="com.goodee.JoinTree.vo.MeetingRoom">
		SELECT
			room_no roomNo,
			equip_category equipCategory,
			room_name roomName,
			room_Capacity roomCapacity,
			yn_status ynStatus,
			createdate,
			updatedate,
			create_id createId,
			update_id updateId
		FROM meeting_room
		WHERE 1 = 1
        <if test="equipCategory != null and equipCategory != ''"> <!-- 조건 재설정 필요 -->
            AND equip_category = #{equipCategory}
        </if>
        <if test="ynStatus != null and ynStatus != ''">
            AND yn_status = #{ynStatus}
        </if>
	</select>
</mapper>